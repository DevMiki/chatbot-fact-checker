# Copy to .env to override frontend defaults.
# Leave VITE_API_BASE_URL empty to use the dev proxy to /api.
VITE_API_BASE_URL=
# Change only if your backend is not at http://127.0.0.1:8000 in dev.
BACKEND_PROXY_TARGET=http://127.0.0.1:8000
